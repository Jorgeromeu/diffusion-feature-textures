# @package _global_

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      generate_video.feature_blend_alpha: 0.25,0.5,0.75,1.0
      # generate_video/layers: encoder_blocks,decoder_blocks,all_blocks
      # generate_video/timesteps: first_half,second_half,all

dev_run: true